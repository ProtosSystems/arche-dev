# Authentication

Canonical onboarding auth path for external developers:

- Use `X-Api-Key` on API requests to `api.arche.fi`.

```http
X-Api-Key: <YOUR_API_KEY>
X-Request-ID: your_request_id
```

Use this header set consistently in quickstart, onboarding, and API examples.
